---
interface Props {
  eyebrow: string;
  title: string;
  summary?: string;
}

const { eyebrow, title, summary } = Astro.props;
---

<div class="section-heading">
  <p class="eyebrow">{eyebrow}</p>
  <h2>{title}</h2>
  {summary && <p>{summary}</p>}
</div>
